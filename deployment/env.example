# Optional: load in shell or keeper (copy to .env and fill — .env is gitignored)
# Cadence / Flow
FLOW_NETWORK=testnet
FLOW_SIGNER=testnet-acc
DEPLOYER_ADDRESS=0x

# From deployment.local.json after deploy
LSP_VAULT_EVM=0x
BRIDGED_STFLOW_ERC20=0x
STFLOW_VAULT_TYPE_ID=

# Flow EVM RPC (keeper / cast)
FLOW_EVM_RPC_URL=https://testnet.evm.nodes.onflow.org
